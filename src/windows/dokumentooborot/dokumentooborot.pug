extends ../../../pug/layout/_layout.pug
include ../../../pug/layout/_head.pug
include ../../../pug/ui/_breadcrumbs.pug

block scripts
    script(type="module", src=`./src/windows/dokumentooborot/js/renderer.js`)


block head
  +head('AdamWeb', '')

block content
    main(class="p-4 bg-gray-200 dark:bg-black h-screen w-full overflow-hidden grid grid-cols-auto-1fr gap-12")
      include ../../../pug/Components/_nav.pug
      div(class="flex flex-col gap-4 h-[calc(100vh-2rem)]")
        div(class="flex items-center gap-4 p-4 rounded-3xl min-h-16 w-full bg-black dark:bg-gray-200")
          div(class="flex flex-wrap gap-2")
            -
              var items = [
                {
                  name: "Home",
                  link: "index.html",
                },
                {
                  name: "Документооботот",
                  link: "catalogNews.html",
                },
              ]
            +breadcrumbs(items)
        div(class="relative flex flex-col gap-4 p-4 rounded-3xl min-h-16 w-full bg-black dark:bg-gray-200 h-full")
          div(Nav="", class="flex flex-wrap items-center gap-4")
          div(class="@container/page w-full h-full scrollbar scrollbar-light dark:scrollbar-dark overflow-y-auto overflow-x-hidden")
            div(Body="", class="grid @lg/page:grid-cols-2 @[48rem]/page:grid-cols-4 @[90rem]/page:grid-cols-6 gap-4 min-h-28")
            div(loading="", class="flex flex-col items-center justify-center w-full h-full absolute z-10 left-0 top-0 rounded-3xl after:content-[''] after:w-full after:h-full after:absolute after:left-0 after:top-0 after:blur-md")
              div(class="loader loader-light dark:loader-dark relative w-24 h-24 rounded-full")
